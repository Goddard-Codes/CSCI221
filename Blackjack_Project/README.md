Evan's solo project for '25 Fall semester Data Structures Course
To run the main program use the following two commands (using the Makefile):
make
./final_main chips.txt
To run the testing program use the following commands:
g++ -std=c++14 tester.cpp deck.cpp card.cpp node.cpp queue.cpp hand.cpp -o tester
./tester